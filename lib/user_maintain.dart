import 'package:flutter/material.dart';

class UserMaintain extends StatelessWidget {
  const UserMaintain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("聯絡資訊修改"),
        
      ),
    );
  }
}
