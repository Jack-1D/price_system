import 'package:flutter/material.dart';

class ShowAssignedQuotation extends StatelessWidget {
  const ShowAssignedQuotation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("報價總覽"),
        
      ),
    );
  }
}