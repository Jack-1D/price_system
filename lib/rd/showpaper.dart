import 'package:flutter/material.dart';

class ShowPaper extends StatelessWidget {
  const ShowPaper({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("圖紙總覽"),
        
      ),
    );
  }
}