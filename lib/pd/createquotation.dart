import 'package:flutter/material.dart';

class CreateQuotation extends StatelessWidget {
  const CreateQuotation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("新增報價"),
        
      ),
    );
  }
}